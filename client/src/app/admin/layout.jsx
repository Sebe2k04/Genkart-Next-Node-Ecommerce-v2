
// pages/dashboard.js (protected route)


export default function Layout({children,params}) {

    return (
        <section>
            {children}
            {/* <AdminGuard/> */}
            
        </section>
    );
}